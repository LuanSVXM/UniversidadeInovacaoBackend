export const APP_ROUTES = {
  StudentRoute:               "/students",
  EmployeeRoute:              "/employee",
  AuthRoute:                  "/auth",
};

const NameRoutes = {
  ...APP_ROUTES,
};

export default NameRoutes;
